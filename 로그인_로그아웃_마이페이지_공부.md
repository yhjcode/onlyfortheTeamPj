# BGGChef 로그인 / 로그아웃 / 마이페이지 코드 공부

> 이 문서는 전체 흐름을 **층(Layer)** 별로 나눠서 설명합니다.  
> 코드를 보기 전에 "왜 이런 구조인지"를 먼저 이해하는 게 포인트예요.

---

## 1. 전체 구조 한눈에 보기 (MVC 패턴)

```
브라우저 (사용자)
    ↓  HTTP 요청
[LoginFilter]  ← 문지기. 로그인 안 했으면 차단
    ↓
[UserController]  ← 교통 정리. 요청을 받아서 누구한테 넘길지 결정
    ↓
[UserService]  ← 두뇌. 비즈니스 규칙 처리 (아이디 중복 체크 등)
    ↓
[UserDAO]  ← 손발. DB에 실제로 SQL을 날린다
    ↓
[Oracle DB]  ← 실제 데이터가 저장된 곳
    ↑
[UserDTO]  ← 데이터 운반용 택배 상자. 계층 간 데이터를 담아서 이동
```

파일 위치 정리:
| 파일 | 위치 | 역할 |
|------|------|------|
| `UserDTO.java` | `dto/` | 회원 정보를 담는 그릇 |
| `UserDAO.java` | `dao/` | DB와 대화 |
| `UserService.java` | `service/` | 비즈니스 로직 |
| `UserController.java` | `controller/` | URL 요청 처리 |
| `LoginFilter.java` | `filter/` | 로그인 체크 문지기 |

---

## 2. UserDTO — 데이터 운반용 상자

**파일:** `com/bggchef/dto/UserDTO.java`

```java
public class UserDTO {
    private String userId;      // 아이디 (예: "hong123")
    private String email;       // 이메일
    private String password;    // 비밀번호
    private String nickname;    // 닉네임
    private String phone;       // 전화번호
    private String profileImg;  // 프로필 이미지 경로
    private Date   birthday;    // 생년월일
    private int    isDeleted;   // 0=정상, 1=탈퇴
    private String medalGrade;  // 메달 등급
    
    // ... getter/setter ...
}
```

### DTO가 뭔가요?
- Data Transfer Object의 약자
- **DB → DAO → Service → Controller → JSP** 로 데이터를 넘길 때 쓰는 **택배 상자**
- 로직이 없고 그냥 데이터만 담는다

### 왜 쓰나요?
- `selectById()`에서 DB에서 꺼낸 회원정보 7개를 변수 7개로 넘기면 너무 복잡함
- DTO 하나에 다 담아서 넘기면 깔끔함

---

## 3. UserDAO — DB와 직접 대화하는 곳

**파일:** `com/bggchef/dao/UserDAO.java`

이 클래스의 역할은 딱 하나: **SQL을 실행하고 결과를 DTO로 반환**

### 3-1. selectById() — 로그인할 때 회원 조회

```java
public UserDTO selectById(String userId) throws SQLException {
    String sql = "SELECT user_id, email, password, nickname, phone, profile_img, "
               + "       birthday, is_deleted, medal_grade "
               + "  FROM USERS WHERE user_id = ? AND is_deleted = 0";
    // ...
}
```

**흐름:**
```
1. SQL 준비: "user_id = ? AND is_deleted = 0"
   → is_deleted = 0 조건 덕분에 탈퇴한 회원은 조회 안 됨!

2. ?에 userId 값 넣기 (PreparedStatement)

3. DB 실행 → 결과(ResultSet) 받기

4. rs.next()로 행이 있으면 UserDTO에 값 채워서 반환
   행이 없으면 null 반환 (→ 없는 아이디라는 뜻)

5. finally에서 conn, pstmt, rs 반드시 닫기
```

> **왜 `?`를 쓰나요?**  
> SQL Injection 공격을 막기 위해서입니다.  
> `"WHERE user_id = '" + userId + "'"` 이렇게 쓰면 해킹 당합니다.  
> `?`와 `PreparedStatement`를 쓰면 자동으로 안전하게 처리됩니다.

### 3-2. insert() — 회원가입

```java
String sql = "INSERT INTO USERS "
           + "(user_id, email, password, ...) "
           + "VALUES (?, ?, ?, ...)";
```

`pstmt.setString(1, u.getUserId())` 처럼 순서대로 `?`에 값을 넣습니다.  
`executeUpdate()`를 호출하면 INSERT가 실행되고, 영향받은 행 수를 반환합니다.

### 3-3. existsId() — 아이디 중복 체크

```java
String sql = "SELECT 1 FROM USERS WHERE user_id = ?";
// ...
return rs.next(); // 행이 있으면 true (= 이미 존재함)
```

`SELECT 1`은 "그냥 뭔가 있는지만 체크"할 때 씁니다.  
실제 컬럼 값이 필요 없고 존재 여부만 확인할 때 성능상 유리합니다.

### 3-4. update() — 회원정보 수정

```java
String sql = "UPDATE USERS SET email=?, nickname=?, phone=?, birthday=?, profile_img=? "
           + "WHERE user_id=?";
```

비밀번호는 여기서 수정하지 않습니다 (비밀번호 변경은 별도 기능으로 분리).

### 3-5. deleteLogically() — 회원 탈퇴 (논리 삭제)

```java
String sql = "UPDATE USERS SET is_deleted = 1 WHERE user_id = ?";
```

**논리 삭제(Logical Delete)**: 행을 실제로 지우지 않고 `is_deleted = 1`로만 바꿉니다.  
- 장점: 데이터 복구 가능, 통계/로그 보존
- `selectById()`에서 `is_deleted = 0` 조건으로 탈퇴 회원은 자동 제외됨

### 3-6. upsertIntro() — 셰프 소개글 등록/수정

```java
String sql = "MERGE INTO CHEF_INTRO ci USING DUAL ON (ci.user_id = ?) "
           + "WHEN MATCHED    THEN UPDATE SET ci.intro = ?, ci.updated_at = SYSDATE "
           + "WHEN NOT MATCHED THEN INSERT (user_id, intro, updated_at) VALUES (?, ?, SYSDATE)";
```

**MERGE = UPSERT**: 있으면 UPDATE, 없으면 INSERT를 한 SQL로 처리합니다.  
Oracle의 특수 문법이며 `USING DUAL`은 "가상 테이블을 기준으로 비교"하라는 뜻입니다.

---

## 4. UserService — 비즈니스 로직

**파일:** `com/bggchef/service/UserService.java`

DAO를 직접 호출하기 전에 **업무 규칙을 적용**하는 계층입니다.

### 4-1. login() — 로그인 검증

```java
public UserDTO login(String userId, String password) throws SQLException {
    UserDTO user = userDAO.selectById(userId);   // DB에서 회원 조회
    if (user != null && user.getPassword().equals(password)) {
        return user;   // 비밀번호 일치 → 회원 정보 반환
    }
    return null;       // 불일치 → null 반환
}
```

**흐름:**
```
1. userId로 DB 조회 → UserDTO 반환 (없으면 null)
2. user가 null이 아니고 & 비밀번호가 같으면 → 로그인 성공
3. 아니면 → null 반환 (Controller에서 실패 처리)
```

> **현재 코드의 한계**: 비밀번호를 평문 비교하고 있습니다.  
> 실제 서비스라면 DB에 해시된 비밀번호를 저장하고  
> `PasswordUtil.encrypt(password).equals(user.getPassword())`로 비교해야 합니다.

### 4-2. join() — 회원가입 (중복 체크 포함)

```java
public boolean join(UserDTO user) throws SQLException {
    if (userDAO.existsId(user.getUserId())) {
        return false;  // 이미 존재하는 아이디
    }
    return userDAO.insert(user) > 0;  // INSERT 성공 여부
}
```

아이디 중복 체크 → INSERT 순서입니다. Service가 이 두 단계를 조율합니다.

### 4-3. withdraw() — 회원 탈퇴

```java
public boolean withdraw(String userId) throws SQLException {
    return userDAO.deleteLogically(userId) > 0;
}
```

단순히 DAO를 호출하는 것처럼 보이지만, 나중에 "탈퇴 전 이메일 발송", "포인트 환불" 같은  
비즈니스 로직이 추가된다면 Service 계층에서 처리합니다.

---

## 5. UserController — 요청 교통 정리

**파일:** `com/bggchef/controller/UserController.java`

```java
@WebServlet("/user/*")
@MultipartConfig
public class UserController extends HttpServlet {
```

- `@WebServlet("/user/*")`: `/user/`로 시작하는 모든 URL을 이 클래스가 처리
- `@MultipartConfig`: 파일 업로드(프로필 이미지)를 받기 위해 필요

### URL과 처리 매핑

| URL | HTTP 메서드 | 처리 메서드 |
|-----|------------|------------|
| `/user/login` | GET | 로그인 페이지 표시 |
| `/user/login` | POST | 로그인 처리 (`doLogin`) |
| `/user/logout` | GET | 로그아웃 처리 |
| `/user/join` | GET | 회원가입 페이지 표시 |
| `/user/join` | POST | 회원가입 처리 (`doJoin`) |
| `/user/mypage` | GET | 마이페이지 표시 |
| `/user/edit` | GET/POST | 정보 수정 |
| `/user/withdraw` | GET/POST | 회원 탈퇴 |
| `/user/checkId` | GET | 아이디 중복 체크 (AJAX) |

### 5-1. 어떻게 URL을 구분하나요?

```java
protected void doGet(HttpServletRequest req, HttpServletResponse resp) {
    String action = req.getPathInfo();  // "/user/mypage"에서 "/mypage" 부분만 꺼냄

    if ("/login".equals(action)) {
        // 로그인 페이지
    } else if ("/mypage".equals(action)) {
        // 마이페이지
    } else if ("/logout".equals(action)) {
        // 로그아웃
    }
    // ...
}
```

`getPathInfo()`는 서블릿 매핑(`/user/`) 뒤의 경로만 반환합니다.  
`/user/mypage` → `action = "/mypage"`

### 5-2. 로그인 처리 — doLogin()

```java
private void doLogin(HttpServletRequest req, HttpServletResponse resp) {
    String userId   = req.getParameter("userId");    // 폼에서 입력한 값
    String password = req.getParameter("password");

    UserDTO user = userService.login(userId, password);

    if (user != null) {
        // 로그인 성공: 세션에 저장
        req.getSession().setAttribute("loginUser", user);
        resp.sendRedirect(req.getContextPath() + "/main");
    } else {
        // 로그인 실패: 에러 메시지와 함께 로그인 페이지로
        req.setAttribute("errorMsg", "아이디 또는 비밀번호가 틀렸습니다.");
        req.getRequestDispatcher("/WEB-INF/views/user/login.jsp").forward(req, resp);
    }
}
```

**세션(Session)이 뭔가요?**
```
세션 = 서버가 이 브라우저를 기억하는 방법

로그인 전: 서버는 브라우저를 모름 (그냥 낯선 손님)
로그인 후: req.getSession().setAttribute("loginUser", user)
           → 서버의 세션 저장소에 "이 브라우저 = hong123" 기록
           → 이후 모든 요청에서 session.getAttribute("loginUser")로 꺼낼 수 있음
로그아웃:  session.invalidate() → 세션 삭제 (서버가 잊어버림)
```

### 5-3. 로그아웃 처리

```java
} else if ("/logout".equals(action)) {
    req.getSession().invalidate();   // 세션 전체 삭제
    resp.sendRedirect(req.getContextPath() + "/main");
}
```

`invalidate()`는 세션에 저장된 모든 정보를 지우고 세션 자체를 무효화합니다.  
이후 `session.getAttribute("loginUser")`를 하면 null이 반환됩니다.

### 5-4. 마이페이지 처리 — GET /user/mypage

```java
} else if ("/mypage".equals(action)) {
    // 1. 로그인 체크
    HttpSession session = req.getSession(false);
    if (session == null || session.getAttribute("loginUser") == null) {
        resp.sendRedirect(req.getContextPath() + "/user/login");
        return;
    }

    // 2. 세션에서 로그인 유저 꺼내기
    UserDTO loginUser = (UserDTO) session.getAttribute("loginUser");

    // 3. 마이페이지에 필요한 데이터 조회
    req.setAttribute("myRecipes",   recipeDAO.selectByUserId(loginUser.getUserId()));
    req.setAttribute("myReviews",   reviewDAO.selectByUserId(loginUser.getUserId()));
    req.setAttribute("myFavorites", favoriteDAO.selectByUserId(loginUser.getUserId()));
    req.setAttribute("myThemes",    themeDAO.selectByUserId(loginUser.getUserId()));
    req.setAttribute("chefIntro",   userService.getIntro(loginUser.getUserId()));

    // 4. JSP에 전달
    req.setAttribute("contentPage", "/WEB-INF/views/user/mypage.jsp");
    req.getRequestDispatcher("/WEB-INF/views/common/layout.jsp").forward(req, resp);
}
```

**`getSession(false)` vs `getSession()`의 차이:**
- `getSession()` = 세션 없으면 새로 만들어서 반환
- `getSession(false)` = 세션 없으면 null 반환 (새로 만들지 않음)
- 로그인 체크할 때는 반드시 `getSession(false)` 사용!

**`forward` vs `sendRedirect`의 차이:**
```
forward: 서버 내부에서 JSP로 넘김 (URL이 안 바뀜, req/resp 공유)
         req.setAttribute로 담은 데이터를 JSP에서 읽을 수 있음
         
sendRedirect: 브라우저에게 "이 URL로 다시 요청해" 라고 알림 (URL이 바뀜)
              req.setAttribute 데이터는 사라짐
              주로 로그인 후, 로그아웃 후 페이지 이동에 사용
```

### 5-5. 회원가입 처리 — doJoin()

```java
// 폼 데이터 → UserDTO에 담기
UserDTO user = new UserDTO();
user.setUserId(req.getParameter("userId"));
user.setEmail(req.getParameter("email"));
// ...

// 프로필 이미지 파일 처리
Part filePart = req.getPart("profileImg");
String originalName = filePart.getSubmittedFileName();
if (originalName != null && !originalName.isEmpty()) {
    String savedName = FileUtil.generateUniqueFileName(originalName);  // UUID로 파일명 변환
    filePart.write(uploadDir + "/" + savedName);                        // 서버에 저장
    user.setProfileImg("/resources/upload/profile/" + savedName);      // 경로를 DTO에
}
```

파일 업로드 처리는:
1. `@MultipartConfig`가 있어야 `req.getPart()`를 쓸 수 있음
2. 원본 파일명을 그대로 쓰면 겹칠 수 있어서 `FileUtil`이 UUID로 새 이름 생성
3. 파일을 서버 디스크에 저장하고, DB에는 경로 문자열만 저장

### 5-6. 회원탈퇴 처리 — doWithdraw()

```java
private void doWithdraw(HttpServletRequest req, HttpServletResponse resp) {
    UserDTO loginUser = (UserDTO) session.getAttribute("loginUser");
    String password = req.getParameter("password");

    // 비밀번호 재확인 (login 메서드를 재활용)
    UserDTO verified = userService.login(loginUser.getUserId(), password);
    if (verified == null) {
        // 비밀번호 틀림 → 에러 표시
        return;
    }

    // 비밀번호 맞으면 탈퇴 처리
    userService.withdraw(loginUser.getUserId());
    session.invalidate();  // 로그아웃도 함께 처리
    resp.sendRedirect(req.getContextPath() + "/main");
}
```

탈퇴 전에 비밀번호를 한 번 더 확인합니다. `userService.login()`을 재활용해서 검증합니다.

---

## 6. LoginFilter — 로그인 안 한 사람 차단

**파일:** `com/bggchef/filter/LoginFilter.java`

필터는 **서블릿보다 먼저 실행**되는 코드입니다. 문지기 역할입니다.

```java
public void doFilter(ServletRequest req, ServletResponse res, FilterChain chain) {
    HttpSession session = request.getSession(false);
    Object loginUser = (session != null) ? session.getAttribute("loginUser") : null;

    if (loginUser == null) {
        // 로그인 안 했으면 로그인 페이지로 강제 이동
        response.sendRedirect(request.getContextPath() + "/user/login");
        return;
    }

    // 로그인 했으면 원래 요청 계속 진행
    chain.doFilter(req, res);
}
```

`chain.doFilter(req, res)`: "통과! 다음으로 가도 됩니다" 라는 뜻

> **web.xml에서 어떤 URL에 이 필터를 적용할지 설정**합니다.  
> 예: `/recipe/write`, `/user/mypage` 등에만 적용하면  
> 로그인 없이 글쓰기 페이지 접근 시 자동으로 로그인 페이지로 보냅니다.

---

## 7. 전체 흐름 요약 — 로그인 시나리오

```
[사용자] /user/login 페이지 접속 (GET)
   ↓
[UserController.doGet] action = "/login"
   → login.jsp를 layout.jsp에 끼워서 표시
   
[사용자] 아이디/비밀번호 입력 후 제출 (POST)
   ↓
[UserController.doPost] action = "/login"
   → doLogin() 호출
      ↓
   [UserService.login("hong123", "1234")]
      ↓
   [UserDAO.selectById("hong123")]
      → DB에서 hong123 조회 → UserDTO 반환
      ↑
   비밀번호 비교: user.getPassword().equals("1234")
      ↑
   일치하면 UserDTO 반환, 불일치하면 null
      ↑
   user != null → 세션에 loginUser 저장
   resp.sendRedirect("/main")
   
[사용자] 메인 페이지 이동 완료 ✓
```

---

## 8. 자주 헷갈리는 개념 정리

| 개념 | 설명 | 예시 |
|------|------|------|
| **Session** | 서버가 특정 브라우저를 기억하는 저장소 | `session.setAttribute("loginUser", user)` |
| **forward** | 서버 내부에서 JSP로 데이터 전달 | `req.getRequestDispatcher("...").forward(...)` |
| **redirect** | 브라우저에게 다른 URL로 재요청 지시 | `resp.sendRedirect("/main")` |
| **getAttribute / setAttribute** | req/session에 데이터 보관함 | `req.setAttribute("errorMsg", "...")` |
| **getPathInfo()** | URL의 서블릿 매핑 이후 경로 | `/user/login` → `/login` |
| **PreparedStatement** | SQL 인젝션 방지용 SQL 실행기 | `pstmt.setString(1, userId)` |
| **논리 삭제** | DB 행을 지우지 않고 플래그만 변경 | `is_deleted = 1` |
| **MERGE (UPSERT)** | 있으면 UPDATE, 없으면 INSERT | `MERGE INTO ... USING DUAL` |

---

## 9. 코드 읽는 순서 추천

처음 공부할 때는 이 순서로 읽으세요:

1. **`UserDTO.java`** — 어떤 데이터를 다루는지 파악
2. **`UserDAO.java`** — SQL이 어떻게 생겼는지 확인
3. **`UserService.java`** — 비즈니스 규칙 이해
4. **`UserController.java`** — URL 흐름 파악
5. **`LoginFilter.java`** — 보안 체크 이해

그 다음에 JSP 파일을 보면서 "Controller가 넘긴 데이터를 JSP에서 어떻게 출력하는지" 보면 완성입니다.
